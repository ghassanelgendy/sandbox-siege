{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git add -A && git diff --cached --quiet || git commit -m \"Update $(git diff --cached --name-only | sed 's|^backend/siege/||; s|^frontend/src/||' | paste -sd ',' - | sed 's/,/, /g')\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
