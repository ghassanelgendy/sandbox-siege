# Sandbox Siege Container Environment Configuration

# LocalStack Auth Token (Required for Ultimate-tier IAM/RDS/EC2)
LOCALSTACK_AUTH_TOKEN=your_localstack_token_here

# LLM Provider API Keys
BYNARA_API_KEY=your_bynara_key_here
DAHL_API_KEY=your_dahl_key_here
GROQ_API_KEY=your_groq_key_here

# Resend Email Configuration (Report dispatch on CI gate)
RESEND_API_KEY=your_resend_api_key_here
REPORT_EMAIL_TO=ghassanelgendyy@gmail.com,ahmedwagdymohy@gmail.com
REPORT_EMAIL_FROM=sandbox@ghassan.online

# Cloudflare Tunnel Token (For custom domain access)
# If using a Named Tunnel token from Cloudflare Zero Trust:
CLOUDFLARE_TUNNEL_ID=1589645d-cd7b-43da-83b3-a2934faabdc8
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here
CLOUDFLARE_TUNNEL_COMMAND=tunnel --no-autoupdate run --token

# SearXNG — backs the agent's `web_search` tool (PRD FR-3.5).
# Optional: the committed searxng/settings.yml already carries a generated key.
# Set this to override it in a shared or public deployment.
SEARXNG_SECRET=

# Where the backend reaches SearXNG. Inside compose this is the service name;
# override to http://localhost:18080 when running the backend outside Docker.
SIEGE_SEARXNG_URL=http://searxng:8080
# /demo shows a finished run for this many seconds, then returns to idle (PRD D-60)
SIEGE_DEMO_RESULT_TTL_S=600

# Jev (TypeSafe System One) advisory judge — optional (PRD §8.2, FR-4.9, D-43/D-44).
# Reached through the jev-sidecar container, which wraps Vercel AI Gateway's
# `experimental_evaluate` SDK call for the `typesafe-ai/jev` model (no direct
# REST endpoint for Jev is documented/verified — see jev-sidecar/README.md).
# Leave AI_GATEWAY_API_KEY or JEV_API_KEY blank to disable Jev entirely;
# jev.py falls back to None cleanly and deterministic detectors still gate the run.
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_key_here
# Shared secret between siege-backend and jev-sidecar (any random string you pick).
JEV_API_KEY=your_shared_sidecar_secret_here
